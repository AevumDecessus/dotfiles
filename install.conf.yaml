- defaults:
    link:
      relink: true
- clean: ['~']
- link:
    ~/.dotfiles: ''
    ~/.vimrc: vimrc
    ~/.profile: profile
    ~/.screenrc: screenrc
- shell:
  - [git submodule update --init --recursive, Installing submodules]
